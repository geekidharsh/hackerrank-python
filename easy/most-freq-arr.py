# General
# Find the most frequent integer in an array

def frequent_arr(arr):
	sort_arr = sorted(arr)
	temp = 1
	length = len(sort_arr)
	if temp == sort_arr[length-1]:
		return length
	else: 
		while temp ==:
			pass
num_arr = [1,1,1,2,2]
print frequent_arr(num_arr)

